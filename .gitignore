# DB 접속정보/JWT 비밀키가 들어있는 실제 설정 파일 (application-example.yml을 복사해서 각자 만들 것)
backend/src/main/resources/application.yml

# Maven 빌드 산출물
backend/target/

# 로컬 개발 단계에서 업로드된 파일 (S3로 옮기면 이 디렉토리 자체가 없어짐)
backend/uploads/

# IDE
.vscode/
.idea/
*.iml

# OS
.DS_Store
Thumbs.db

# 로그
*.log
